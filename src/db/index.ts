import type { Database } from "./init";

import type {
  AllowedData,
  DatabaseCollectionOptions,
  DatabaseObserverCallback,
  DatabaseOptions,
  Db,
  DbObserverCallback,
  DbCollectionOptions,
  DbObserverEvent,
  Instance,
  ValueOf,
} from "./types";

export type {
  Database,
  AllowedData,
  DatabaseCollectionOptions,
  DatabaseObserverCallback,
  DatabaseOptions,
  Db,
  DbObserverCallback,
  DbCollectionOptions,
  DbObserverEvent,
  Instance,
  ValueOf,
};

/**
 * Node-specific storage exports are introduced with the durable-file milestone.
 * This module exists now so the package boundary is explicit and testable.
 */
export const nodeStorageApiVersion = 1 as const;
